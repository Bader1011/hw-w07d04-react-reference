import React, { Component } from 'react';


class JSXInfo extends Component {
    render() {
        return (


            <div className="JSXInfo" >
                
                <h2 > JSXInfoe </h2>
                <h5> To work with React you have to find a syntax extension to JavaScript called JSX, which looks like an XML tag (but is not). This syntax extension describes the look of the UI. JSX is the alternative of React.createElements. It is a JavaScript extension that allows us to define React elements using syntax that looks similar to HTML. </h5>
            </ div>
        )
    }
}


    export default JSXInfo;